# Codex CLI (OpenAI) registers MCP servers in ~/.codex/config.toml.
# Add this block, replacing the two absolute paths with your clone's location.
[mcp_servers.kestrel]
command = "/ABSOLUTE/PATH/mcp-skills-finetuning/.venv/bin/python"
args = ["/ABSOLUTE/PATH/mcp-skills-finetuning/mcp/kestrel_mcp_server.py"]
